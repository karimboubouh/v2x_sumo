"""D-PSGD algorithm configuration.

D-PSGD has no algorithm-specific parameters beyond what is set in the
global DL_CFG (config.py).  This file exists as the canonical template
for new algorithms — add parameters here when implementing a new algorithm
under algorithms/<name>/config.py.
"""
